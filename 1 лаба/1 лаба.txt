// Задание 1
СтрокаСКавычками = """Это текст""";
СтрокаБезКавычек = "Это текст";

Сообщить("С кавычками: " + СтрокаСКавычками);
Сообщить("Без кавычек: " + СтрокаБезКавычек);

// Задание 2
ОднострочнаяСтрока = "Это текст";
МногострочнаяСтрока = "Это" + Символы.ПС + "Текст";

Сообщить("Однострочный вывод: " + ОднострочнаяСтрока);
Сообщить("Многострочный вывод: " + МногострочнаяСтрока);

// Задание 3
Имя = "Иван";
Возраст = 20;

Конкатенация = "Имя: " + Имя + ", Возраст: " + Строка(Возраст);
Шаблон = "Имя: %1, Возраст: %2";
СтрокаИзШаблона = СтрШаблон(Шаблон, Имя, Возраст);

Сообщить("Результат конкатенации: " + Конкатенация);
Сообщить("Результат использования СтрШаблон: " + СтрокаИзШаблона);

// Числа задание 1
Стоимость = 1200; 
СкидкаПроцент = 7; 
Скидка = Стоимость * СкидкаПроцент / 100;
ИтоговаяСумма = Стоимость - Скидка;

Сообщить("Сумма к оплате: " + ИтоговаяСумма);

// Числа задание 2
Переменная1 = 20;
Переменная2 = 5;

Числитель = -(Переменная1 + Переменная2) / Переменная2 * Переменная1;
Знаменатель = Переменная2 - Переменная1;
Результат = Числитель % Знаменатель;

Сообщить("Переменная 1: " + Переменная1);
Сообщить("Переменная 2: " + Переменная2);
Сообщить("Результат выражения: " + Результат);

// Дата задание 1
ИсходнаяДата = Дата(2004, 6, 16);
СекундВДнях = 125 * 24 * 60 * 60;
НоваяДата = ИсходнаяДата - СекундВДнях;

Сообщить("Исходная дата: " + ИсходнаяДата);
Сообщить("Дата после вычитания 125 дней: " + НоваяДата);

// Дата задание 2
ИсходнаяДата = ТекущаяДата(); // Текущая дата
СекундВМесяцах = 9 * 30 * 24 * 60 * 60;
НоваяДата = ИсходнаяДата - СекундВМесяцах;

Сообщить("Исходная дата: " + ИсходнаяДата);
Сообщить("Дата после вычитания 9 месяцев: " + НоваяДата);

// Дата задание 3
День = 16;
Месяц = 6;
Год = 2004;
ИсходнаяДата = Дата(Год, Месяц, День);
СекундВГодах = 7 * 365 * 24 * 60 * 60;
НоваяДата = ИсходнаяДата + СекундВГодах;

Сообщить("Исходная дата: " + ИсходнаяДата);
Сообщить("Дата после прибавления 7 лет: " + НоваяДата);

// Булево задание 1
Результат = (Истина Или Ложь) И (Истина И Ложь);
Сообщить("Результат выражения: " + Результат);

// Булево задание 2
Результат = НЕ (Истина И Ложь) Или ((Истина И Истина И Ложь) Или НЕ Истина);
Сообщить("Результат выражения: " + Результат); 

// Булево задание 3
ЛеваяЧасть = (4375 / 16 * 0.9);
ПраваяЧасть = -(675 + 435 / 100 * (-73));
Результат = (ЛеваяЧасть > ПраваяЧасть) И НЕ Ложь;
Сообщить("Результат выражения: " + Результат);






